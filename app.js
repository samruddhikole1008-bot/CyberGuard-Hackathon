const tabs=document.querySelectorAll('.tab');tabs.forEach(b=>b.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelectorAll('.panel').forEach(p=>p.classList.add('hidden'));document.getElementById(b.dataset.tab).classList.remove('hidden')}));
function resultHTML(d){const cls=d.level.toLowerCase().replace(' ','-');return `<div class="score-row"><div><small>RISK LEVEL</small><strong class="${cls}">${d.level}</strong></div><div class="score">${d.score}<span>/100</span></div></div><div class="meter"><span style="width:${d.score}%"></span></div><h3>Why this result?</h3><ul>${d.reasons.map(r=>`<li>${r}</li>`).join('')}</ul>${d.domain?`<div class="details"><span>Domain: <b>${d.domain}</b></span><span>Protocol: <b>${d.protocol}</b></span><span>Length: <b>${d.length}</b></span></div>`:''}`}
async function send(url,body){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});return r.json()}
document.getElementById('urlBtn').onclick=async()=>{const r=document.getElementById('urlResult');r.classList.remove('hidden');r.innerHTML='<p>Analyzing...</p>';r.innerHTML=resultHTML(await send('/api/scan-url',{url:document.getElementById('urlInput').value}))};
document.getElementById('textBtn').onclick=async()=>{const r=document.getElementById('textResult');r.classList.remove('hidden');r.innerHTML='<p>Analyzing...</p>';r.innerHTML=resultHTML(await send('/api/scan-text',{text:document.getElementById('textInput').value}))};
